# Auth API (Spring). Default is Railway production. Override for local dev (no trailing slash).
# VITE_API_URL=https://floodreliefbackend-production.up.railway.app
VITE_API_URL=http://localhost:8080

# Prediction API (FastAPI). No trailing slash.
VITE_PREDICTION_API_URL=http://localhost:8000
